# elastic-integration-splunk-hec
Collects and parses JSON events from Splunk's HTTP Event Collector (HEC). Events can be delivered via Splunk Ingest Actions (filestream or S3) or posted directly to the HTTP endpoint.
